#!/bin/bash

set -ex

if [[ "$OSTYPE" == "darwin"* ]]; then
  # Mac
  MINOTAUR_SHAREDLIB=minotaur.dylib
else
  # Linux
  MINOTAUR_SHAREDLIB=minotaur.so
fi


@LLVM_BINARY_DIR@/bin/opt -enable-new-pm=0 -load=@PROJECT_BINARY_DIR@/$MINOTAUR_SHAREDLIB -so $@ -S -o -